# bv (beads viewer) local config and caches
.bv/

# Local build, release, and macOS metadata
.derived/
.dist/
.DS_Store
xcuserdata/
*.xcuserstate

# Local Whisper model binaries are large and should not be pushed to GitHub.
MeetlessApp/Resources/Models/*.bin
